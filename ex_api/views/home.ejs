<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 text-center">
                <h2 class="alert-secondary p-3">พยากรณ์อากาศ</h2>
            </div>
        </div>
        <div class="row mt-2">
            <hr />
        </div>
        <div class="row text-center">
            <div class="col-md-3 d-sm-none d-md-block"></div>
            <div class="col-sm-6 col-md-3 mx-md-auto">
                <button type="button" class="btn btn-lg btn-success p-4 mt-1 mb-1" onclick="city_form()">
                จากชื่อจังหวัดหรืออำเภอ
                </button>
            </div>
            <div class="col-sm-6 col-md-3 mx-md-auto">
                <button type="button" class="btn btn-lg btn-primary p-4 mt-1 mb-1" onclick="location_form()">
                จากพิกัดเส้นรุ้งเส้นแวง
                </button>
            </div>
            <div class="col-md-3 d-sm-none d-md-block"></div>
        </div>
        <div class="row mt-2">
            <hr />
        </div>
        <div class="row" id="city" style="display: none;">
            <div class="col-sm-6 mx-auto">
                <form action="/api/city" id="form_city" class="form text-center">
                <div class="form-group">
                    <label for="city">จากชื่อจังหวัดหรืออำเภอของประเทศไทย</label>
                    <select name="city" class="form-control" id="city">
                        <option value="">เลือก</option>
                        <% lists.forEach(list => { %>
                         <option value="<%= list.name %> "><%= list.name %> </option>
                        <% }) %>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary mb-2">พยากรณ์</button>
                </form>
            </div>
        </div>
        <div class="row" id="location" style="display:none;">
        <div class="col-6 mx-auto">
            <form action="/api/coordinate" id="form_location" class="text-center">
            <div class="form-group">
                <label for="lon">เส้นแวง (Longtitude)</label>
                <input type="number" name="lon" id="lon" step="0.0001" class="form-control" placeholder="0.0000">
            </div>
            <div class="form-group">
                <label for="lat">เส้นรุ้ง (Latitude)</label>
                <input type="number" name="lat" id="lat" step="0.0001" class="form-control" placeholder="0.0000">
            </div>
            <button type="submit" class="btn btn-primary">พยากรณ์</button>
            </form>
        </div>
        </div>
    </div>
    <script>
        function city_form() {
            document.getElementById("city").style.display = "block";
            document.getElementById("location").style.display = "none";
        }
        function location_form(){
            document.getElementById("city").style.display = "none";
            document.getElementById("location").style.display = "block";
        }
    </script>
</body>
</html>